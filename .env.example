# Production Environment Configuration Template
# Copy and modify these values for deployment

# Application Settings
APP_NAME="Case Changer Pro"
APP_ENV=production
APP_KEY=base64:dGVtcG9yYXJ5X2tleV9mb3JfYnVpbGRfcHJvY2Vzcw==
APP_DEBUG=false
APP_TIMEZONE=UTC
APP_URL=https://your-domain.com

# Localization
APP_LOCALE=en
APP_FALLBACK_LOCALE=en

# Security
BCRYPT_ROUNDS=12

# Logging Configuration
LOG_CHANNEL=stderr
LOG_LEVEL=error

# Database (Memory-only for stateless operation)
DB_CONNECTION=sqlite
DB_DATABASE=:memory:

# Session Configuration (CRITICAL - Must use cookies)
SESSION_DRIVER=cookie
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Cache Configuration (No file persistence)
CACHE_STORE=array
CACHE_PREFIX=casechanger_

# Filesystem (Disabled - no file operations)
FILESYSTEM_DISK=null

# Queue Configuration
QUEUE_CONNECTION=sync

# Broadcasting (Not used)
BROADCAST_CONNECTION=null